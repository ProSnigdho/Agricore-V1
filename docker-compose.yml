services:
  db:
    image: postgres:15
    container_name: agricore_db
    environment:
      POSTGRES_DB: agricore
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
